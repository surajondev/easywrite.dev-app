export const decrypt = (cipherText: any) => {
  try {
    return cipherText;
  } catch (error) {
    console.log(error);
  }
};

export const encrypt = (cipherText: any) => {
  try {
    return cipherText;
  } catch (error) {
    console.log(error);
  }
};
